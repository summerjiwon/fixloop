"use client";

import { useEffect, useState } from "react";
import { getInsights, type Insights } from "@/lib/api";

export function InsightsPanel() { const [data, setData] = useState<Insights | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { getInsights().then(setData).catch((error: Error) => setError(error.message)); }, []); if (error) return <main className="mx-auto max-w-4xl p-10 text-red-700">{error}</main>; if (!data) return <main className="mx-auto max-w-4xl p-10">인사이트를 계산하는 중…</main>; return <main className="mx-auto min-h-screen max-w-4xl px-5 py-10"><p className="font-semibold text-brand">공간기록 인사이트</p><h1 className="mt-1 text-3xl font-bold">최근 {data.period_days}일 운영 현황</h1><section className="mt-8 rounded-xl bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">총 이슈</p><p className="mt-2 text-4xl font-bold">{data.total_issues}</p></section><section className="mt-5 grid gap-4 md:grid-cols-3">{data.insights.length ? data.insights.map((insight) => <article key={insight.title} className="rounded-xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-brand">{insight.metric}</p><h2 className="mt-2 font-bold">{insight.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{insight.detail}</p></article>) : <p className="text-slate-500">아직 집계할 이슈가 없습니다.</p>}</section><p className="mt-8 text-sm text-slate-500">수치는 API의 결정론적 집계에서 계산되며, AI는 이후 이 검증된 수치의 설명에만 사용됩니다.</p></main>; }
